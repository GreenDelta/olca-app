This project contains a [ANT script](./build.xml) that automatically creates 
the target platform for the openLCA RCP application from web-resources. Just 
run this build script, open the [platform.target](./platform.target) file, 
and click 'Set as target platform' to configure this project as runtime for
the openLCA application.  